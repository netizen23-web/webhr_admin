# === Local Development ===
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=hris_admin_only
APP_SESSION_SECRET=change-this-secret-before-production

# === Railway (auto-provided by MySQL addon) ===
# MYSQL_URL=mysql://user:password@host:port/database
# No need to set DB_HOST, DB_PORT, etc. if MYSQL_URL is set
